{
  "name": "sufal-physics-forum",
  "version": "1.0.0",
  "description": "SufalPhysicsForum website",
  "main": "server/dist/index.js",
  "scripts": {
    "dev": "cd server && npm run dev",
    "build": "cd client && npm install && npm run build && cd .. && mkdir -p server/public && cp -r client/dist/. server/public/ && cd server && npm install --include=dev && npm run build",
    "start": "node server/dist/index.js"
  }
}
