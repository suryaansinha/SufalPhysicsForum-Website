{
  "name": "sufal-physics-forum",
  "version": "1.0.0",
  "description": "SufalPhysicsForum website",
  "main": "server/dist/index.js",
  "scripts": {
    "dev": "cd server && npm run dev",
    "build": "cd client && npm install && npm run build && cd .. && mkdir -p server/public && cp -r client/dist/. server/public/ && cd server && npm install --include=dev && npm run build && (chmod +x node_modules/.prisma/client/*.node node_modules/@prisma/engines/* node_modules/prisma/*.so.node 2>/dev/null || true)",
    "postbuild": "cd server && node -r ./dist/build-migrate-url.js ./node_modules/.bin/prisma migrate deploy || true",
    "start": "node -r ./server/dist/prisma-env.js -r ./server/dist/build-migrate-url.js server/dist/index.js"
  }
}
